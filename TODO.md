# TODO: инструменты видеоархива

## Выполнено

- [x] Вынести общие настройки в `config.toml`.
- [x] Использовать единый путь к cookies во всех сетевых скриптах.
- [x] Добавить безопасный `dry-run` по умолчанию в `rename_files.py`.
- [x] Добавить безопасный `dry-run` по умолчанию в `resort.py`.
- [x] Требовать явный `--apply` для переименования и перемещения файлов.
- [x] Исправить BAT-файлы: запуск относительно собственной папки и возврат exit code.
- [x] Добавить таймауты и базовую проверку ошибок внешних программ.
- [x] Сделать папки и языки субтитров параметрами командной строки.

## P0: защита данных

- [x] Добавить журнал операций перемещения `resort.py` в формате JSONL.
  Критерий: каждая операция содержит время, исходный путь, целевой путь и результат.

- [x] Добавить журнал операций переименования `rename_files.py` в формате JSONL.

- [x] Добавить команду отмены последнего запуска `resort.py`.
  Критерий: перемещения восстанавливаются по журналу, конфликтующие пути не затираются.

- [x] Сделать перемещение видео и субтитров транзакционным.
  Критерий: при ошибке все уже выполненные операции для текущего видео откатываются.

- [x] Проверять свободное место и доступность каталога назначения до начала изменений.
  Критерий: `--apply` завершается до первой операции, если проверка не пройдена.

- [x] Добавить подтверждение сводного плана перед массовым `--apply`.
  Критерий: отображается число файлов и папок; для автоматизации есть `--yes`.

- [x] Не считать неизвестный источник окончательным результатом без явного флага.
  Критерий: сетевые ошибки не отправляют видео в `Unknown`; для этого нужен `--allow-unknown`.

## P1: единый интерфейс

- [x] Создать общий CLI `video_tools.py` с подкомандами:
  `download`, `subtitles`, `rename`, `resort`, `duplicates`, `dates`,
  `resolution`, `inventory`.

- [x] Добавить интерактивное меню при запуске `video_tools.py` без аргументов.

- [x] Объединить рабочие Python-программы в один автономный `video_tools.py`.

- [x] Добавить общие параметры `--root`, `--config`, `--verbose`, `--quiet`.

- [x] Проверять конфигурацию отдельной командой `video_tools.py doctor`.
  Критерий: проверяются Python, cookies, `yt-dlp`, `ffprobe`, права записи и структура TOML.

- [x] Добавить выбор папок по маске и списку.
  Примеры: `--folder "Deep Look"`, `--folder-file folders.txt`, `--all-folders`.

- [x] Поддержать переменные окружения для всех чувствительных и машинозависимых путей.

- [x] Добавить `--version`.

- [x] Добавить краткий журнал изменений.

## P1: корректность обработки

- [x] Ограничить проверку дат видеофайлами.

- [x] Проверять дату как календарную, а не только регулярным выражением.

- [x] Унифицировать список видеоформатов во всех скриптах.
  Минимум: `.mp4`, `.mkv`, `.webm`, `.mov`, `.avi`.

- [x] Поддержать субтитры `.vtt`, `.srt`, `.ass` и языковые варианты имён.

- [ ] Улучшить определение YouTube/Rutube ID и исключить ложные ID из других скобок.

- [x] Нормализовать имена Windows:
  зарезервированные имена, завершающие точки/пробелы и максимальную длину пути.

- [x] Не создавать дубли вида `_1`, `_2`, если файлы идентичны.
  Критерий: перед переименованием сравниваются размер и при необходимости SHA-256.

- [x] Добавить отдельный отчёт о видео без ID, даты или автора.

- [ ] Обрабатывать недоступные, приватные и удалённые видео единообразно.

## P1: загрузка

- [ ] Не скрывать важные сообщения `yt-dlp`.
  Критерий: видны предупреждения, повторные попытки, итог и причины пропуска.

- [ ] Добавить итоговую статистику загрузки:
  скачано, пропущено архивом, недоступно, ошибки.

- [ ] Проверять версию `yt-dlp` и предупреждать об устаревшей версии.

- [ ] Настроить повторные попытки и интервалы через `config.toml`.

- [ ] Добавить опциональное ограничение скорости и времени загрузки.

- [ ] Зафиксировать политику форматов.
  Решить, нужен ли строго MP4/H.264/AAC или допустимы контейнеры MKV/WebM.

- [x] Синхронизировать `yt-dlp-archive.txt` с реально существующими видео.
  Удалённые после просмотра видео снова могут быть скачаны, если остаются в
  плейлисте; перед изменением создаётся резервная копия.

- [x] Создать резервную копию `yt-dlp-archive.txt` перед потенциальной правкой.

## P2: производительность

- [ ] Распараллелить `ffprobe` в `check_resolution.py`.
  Критерий: настраиваемое число потоков и стабильный порядок итогового отчёта.

- [x] Кэшировать метаданные `yt-dlp` между запусками в SQLite или JSON.

- [ ] Не обращаться к YouTube повторно для уже известных ID.

- [ ] Добавить пакетный режим отчётов CSV/JSON без цветного терминального вывода.

- [ ] Показывать прогресс для длительных операций.

- [ ] Добавить возможность ограничить обработку:
  `--limit`, `--newer-than`, `--older-than`, `--channel`.

## P2: поиск дублей

- [ ] Искать дубли сначала по YouTube/Rutube ID без сетевого запроса.

- [x] Добавить поиск точных дублей по размеру и SHA-256.

- [ ] Добавить поиск вероятных дублей по нормализованному названию и длительности.

- [ ] Не предлагать удаление автоматически.
  Критерий: формируется только отчёт или явный план с `--apply`.

- [ ] Экспортировать отчёт о дублях в CSV/JSON.

## P2: отчёты об архиве

- [x] Создать команду инвентаризации без чтения видеопотока.
  Поля: папка, имя, расширение, размер, ID, дата, наличие субтитров.

- [ ] Создать отдельный медиаотчёт через `ffprobe`.
  Поля: разрешение, кодек, FPS, длительность, аудиокодек.

- [ ] Выводить каналы без субтитров и неполные пары `video + subtitle`.

- [ ] Находить пустые папки и посторонние служебные файлы.

- [ ] Добавить сводку занимаемого места по каналам и годам.

## P2: тесты и сопровождение

- [x] Создать `tests/` с `unittest` для разбора имён, ID, дат и конфликтов путей.

- [x] Добавить интеграционные тесты на временном каталоге без сети.

- [ ] Подменять `yt-dlp` и `ffprobe` тестовыми заглушками.

- [ ] Добавить проверку форматирования и статический анализ.
  Вариант: Ruff с минимальной конфигурацией.

- [ ] Добавить `requirements.txt` или `pyproject.toml`.

- [x] Создать `README.md` с примерами безопасного запуска и восстановления.

- [ ] Добавить `.gitignore` для `__pycache__`, логов, локальных cookies и кэша.

## Ближайшие шаги

1. Поддержать переменные окружения для путей и чувствительных настроек.
2. Улучшить определение YouTube/Rutube ID и убрать ложные срабатывания.
3. Добавить медиаотчёт через `ffprobe`.
4. Экспортировать отчёты о дублях в CSV/JSON.
5. Добавить `.gitignore` для служебных файлов проекта.

## Следом после этого

1. Добавить итоговую статистику загрузки и не терять важные сообщения `yt-dlp`.
2. Не обращаться к YouTube повторно для уже известных ID, если данные есть в кэше.
3. Добавить пакетный режим отчётов CSV/JSON без терминального шума.
4. Подменять `yt-dlp` и `ffprobe` тестовыми заглушками.
5. Добавить проверку форматирования и статический анализ.

## Идеи без срочности

1. Ограничение обработки по `--limit`, датам и каналу.
2. Поиск вероятных дублей по названию и длительности.
3. Сводка занимаемого места по каналам и годам.
4. Опциональные ограничения скорости и времени загрузки.
5. Формализация политики форматов архива.
